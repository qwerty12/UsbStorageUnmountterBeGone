<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pk.qwerty12.usbstorageunmountterbegone"
    android:versionCode="2"
    android:versionName="1.1" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="15" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name" >
        <meta-data android:name="xposedmodule" android:value="true"/>
        <meta-data android:name="xposedminversion" android:value="2.0*"/>
        <meta-data android:name="xposeddescription" android:value="Releases the UsbStorageUnmountter wakelock after it's created, to workaround a Samsung bug in which it never gets released"/>
    </application>

</manifest>